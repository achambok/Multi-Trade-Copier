# docker-compose.yml — DEPRECATED
#
# Mosquitto is now deployed natively via Homebrew on the Mac Mini host.
# This file is kept for reference only.
#
# To start the native broker:
#   brew services start mosquitto
#
# Config location:
#   /opt/homebrew/etc/mosquitto/mosquitto.conf
#
# See mosquitto-native.conf in the repo root for the config content.
